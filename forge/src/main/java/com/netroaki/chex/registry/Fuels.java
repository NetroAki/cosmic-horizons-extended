package com.netroaki.chex.registry;

public final class Fuels {
  private Fuels() {}
  // Fluids temporarily disabled to get a clean compile for initial test build.
  // We will re-introduce fluid registration with a non-circular pattern after
  // build validation.
}
