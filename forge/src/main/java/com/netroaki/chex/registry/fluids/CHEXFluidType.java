package com.netroaki.chex.registry.fluids;

import net.minecraftforge.fluids.FluidType;

public class CHEXFluidType extends FluidType {

  public CHEXFluidType(Properties properties) {
    super(properties);
  }

  // Custom fluid type implementation
  // We'll add custom behavior later when needed
}
